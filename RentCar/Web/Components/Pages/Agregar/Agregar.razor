@page "/agrega"

@* <div class="form-container">

    <form  @onsubmit:preventDefault="true">

        <div class="form-group">
            <label for="marca">Marca <span class="required">*</span></label>
            <select id="marca" class="form-control"  required>
                <option value="">Seleccionar marca</option>
                
            </select>
        </div>

        <div class="form-group">
            <label for="modelo">Modelo <span class="required">*</span></label>
            <input type="text" id="modelo" class="form-control" 
                   placeholder="Ej: Corolla, Civic, Focus" required />
        </div>

        <div class="form-row">
            <div class="form-group">
                <label for="año">Año <span class="required">*</span></label>
                <input type="number" id="año" class="form-control"  min="2000" max="2025" required />
            </div>
            <div class="form-group">
                <label for="color">Color</label>
                <input type="text" id="color" class="form-control"  placeholder="Ej: Blanco, Negro" />
            </div>
        </div>

        <div class="form-group">
            <label for="placa">Número de Placa <span class="required">*</span></label>
            <input type="text" id="placa" class="form-control" style="text-transform: uppercase;"
                    placeholder="Ej: ABC-123" required />
        </div>

        <div class="form-row">
            <div class="form-group">
                <label for="tipo">Tipo de Vehículo <span class="required">*</span></label>
                <select id="tipo" class="form-control" required>
                    <option value="">Seleccionar tipo</option>
                  
                </select>
            </div>
            <div class="form-group">
                <label for="precio">Precio por Día (DOP) <span class="required">*</span></label>
                <input type="number" id="precio" class="form-control" 
                       step="0.01" min="1" placeholder="3000.00" required />
            </div>
        </div>

        <div class="form-group">
            <label>Imágenes del Vehículo <span class="required">*</span></label>

            <input type="url" id="imagenUrl" name="imagenUrl" class="form-control"
                   placeholder="Ej: https://example.com/imagen.jpg" />

            <div class="image-list" id="imageList" style="margin-top:10px; display:flex; gap:10px; flex-wrap:wrap;"></div>
        </div>

        <div class="btn-container">
            <button type="button" class="btn btn-secondary" >Limpiar</button>
            <button type="submit" class="btn btn-primary">
               
            </button>
        </div>
    </form>
</div> *@

<div class="container">
    <form id="vehicleForm">
        <div class="form-group">
            <label>Marca <span class="required">*</span></label>
            <input type="text" id="marca" name="marca" placeholder="Seleccionar marca" required>
        </div>

        <div class="form-group">
            <label>Modelo <span class="required">*</span></label>
            <input type="text" id="modelo" name="modelo" placeholder="Ej: Corolla, Civic, Focus" required>
        </div>

        <div class="form-row">
            <div class="form-group">
                <label>Año <span class="required">*</span></label>
                <input type="number" id="año" name="año" min="1900" max="2025" required>
            </div>

            <div class="form-group">
                <label>Color</label>
                <input type="text" id="color" name="color" placeholder="Ej: Blanco, Negro">
            </div>
        </div>

        <div class="form-group">
            <label>Placa <span class="required">*</span></label>
            <input type="text" id="placa" name="placa" placeholder="EJ: ABC-123" required>
        </div>

        <div class="form-row">
            <div class="form-group">
                <label>Tipo de Vehículo <span class="required">*</span></label>
                <select id="tipoVehiculo" name="tipoVehiculo" required>
                    <option value="">Seleccionar tipo</option>
                    <option value="Sedan">Sedan</option>
                    <option value="SUV">SUV</option>
                    <option value="Camioneta">Camioneta</option>
                    <option value="Deportivo">Deportivo</option>
                    <option value="Van">Van</option>
                </select>
            </div>

            <div class="form-group">
                <label>Categoría ID <span class="required">*</span></label>
                <input type="number" id="categoriaId" name="categoriaId" min="1" required>
            </div>
        </div>

        <div class="form-group">
            <label>Estado</label>
            <select id="estado" name="estado">
                <option value="Disponible">Disponible</option>
                <option value="Rentado">Rentado</option>
                <option value="Mantenimiento">Mantenimiento</option>
                <option value="No Disponible">No Disponible</option>
            </select>
        </div>

        <div class="form-group">
            <label>URL de Imagen <span class="required">*</span></label>
            <input type="url" id="imageUrl" name="imageUrl" placeholder="Ej: https://example.com/imagen.jpg" required>
        </div>

        <div class="form-group">
            <div class="checkbox-group">
                <input type="checkbox" id="esActivo" name="esActivo" checked>
                <label for="esActivo">Vehículo Activo</label>
            </div>
        </div>

        <div class="button-group">
            <button type="button" class="btn-limpiar" onclick="limpiarFormulario()">LIMPIAR</button>
            <button type="submit" class="btn-submit">GUARDAR</button>
        </div>
    </form>
</div>


@code {

}
